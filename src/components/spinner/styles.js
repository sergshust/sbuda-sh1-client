import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    spinner: {
        margin: "0 auto",
        display: "block",
    },
}));

export default useStyles;
